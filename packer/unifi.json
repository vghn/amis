{
  "variables": {
    "version": "",
    "ansible_repo": ""
  },
  "builders": [
    {
      "name": "Unifi",
      "type": "amazon-ebs",
      "ami_name": "Unifi_{{user `version`}}_{{timestamp}}",
      "ami_description": "Unifi Image ({{user `version`}})",
      "source_ami_filter": {
        "filters": {
          "architecture": "x86_64",
          "root-device-type": "ebs",
          "virtualization-type": "hvm",
          "block-device-mapping.volume-type": "gp2",
          "name": "ubuntu/images/hvm-ssd/ubuntu-bionic-18.04-amd64-server-*"
        },
        "owners": ["099720109477"],
        "most_recent": true
      },
      "instance_type": "t2.micro",
      "region": "us-west-2",
      "ssh_username": "ubuntu",
      "run_tags": {
        "Name": "Unifi AMI",
        "Group": "cosmin",
        "Project": "unifi",
        "Packer": "true"
      },
      "tags": {
        "Name": "Unifi AMI",
        "Group": "cosmin",
        "Project": "unifi",
        "Packer": "true"
      }
    }
  ],
  "provisioners": [
    {
      "type": "shell",
      "inline": "while [ ! -f /var/lib/cloud/instance/boot-finished ]; do echo 'Waiting for cloud-init...'; sleep 5; done"
    },{
      "execute_command": "{{ .Vars }} sudo -E bash -e '{{ .Path }}'",
      "inline": [
        "export DEBIAN_FRONTEND=noninteractive",
        "apt-get update && apt-get -qy -o Dpkg::Options::='--force-confdef' -o Dpkg::Options::='--force-confold' dist-upgrade && apt-get -y autoremove --purge"
      ],
      "type": "shell"
    },{
      "type": "ansible",
      "host_alias": "unifi",
      "groups": [
        "cosmin",
        "aws",
        "docker"
      ],
      "playbook_file": "../ansible/site.yml",
      "ansible_env_vars": [
        "ANSIBLE_SSH_ARGS='-o IdentitiesOnly=yes -o StrictHostKeyChecking=no -o UserKnownHostsFile=/dev/null -o AddKeysToAgent=no'",
        "ANSIBLE_ROLES_PATH=../ansible/roles:../ansible/roles/galaxy",
        "ANSIBLE_REMOTE_TMP=/tmp/${USER}/ansible"
      ]
    }
  ]
}
